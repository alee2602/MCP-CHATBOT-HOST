services:
  - type: web
    name: mcp-color-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python color-mcp/test_server.py
    plan: free
    autoDeploy: true